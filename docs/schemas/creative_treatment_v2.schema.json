{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://minimax-h3-prompt-enhancer.local/schemas/creative-treatment-v2.json",
  "title": "MiniMax H3 Creative Treatment v2",
  "description": "Canonical editable creative-treatment document. Legacy v1 is accepted by the runtime parser only and normalizes in memory to this v2 contract.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion"],
  "properties": {
    "schemaVersion": {"const": 2},
    "contentFormat": {"$ref": "#/$defs/catalogChoice"},
    "genre": {"$ref": "#/$defs/catalogChoice"},
    "visualLanguage": {"$ref": "#/$defs/catalogChoice"},
    "worldAesthetic": {"$ref": "#/$defs/catalogChoice"},
    "tone": {"$ref": "#/$defs/catalogChoice"},
    "titleScreenStyle": {"$ref": "#/$defs/catalogChoice"},
    "animationCadence": {
      "description": "Experimental authored-pose exposure rhythm. Adaptive or omission emits no cadence request and never changes FPS.",
      "enum": ["adaptive", "ones", "twos", "threes"]
    }
  },
  "$defs": {
    "catalogChoice": {
      "description": "A lowercase identifier from the corresponding runtime catalog; use none for no override.",
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-z0-9][a-z0-9_]*$"
    }
  }
}
